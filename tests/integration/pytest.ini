[pytest]
# Cross-package integration suite (run from the repo root via
# `make test-integration`). Uses the workspace venv, which has every
# semos-agentura-* package plus pytest-asyncio installed.
asyncio_mode = auto
